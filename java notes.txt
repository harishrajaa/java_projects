java:

----set the path
-----command line arguments - pass input  when we run the program
                            - arguments read input as strings
		System.out.println(args[0]);

class names----Student, TestStudent
variable names----
	identifiers
	myWidth
method names-----getter and setter
comments--------/**/
java keywords
operators--------arithmetic, logical, unary, relational, comparison, shift, 

flow control system-------
	control flow

arrays=====
arraycopy(source, 0, dest, 0, source.length);

clas============
class<class name>

variables----instance---id-name, salary, local, static

instance variable = belongs to class

object and references============

constructor=====define within the class----type of method---implicitly called---dont have any return types....designed not to return anything.--constructor and object name should be saame----deffault/parameterised/

'this' operator=====using same name---this refers to the current object

main class===class which contains public static void main

static variable = 
